{
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0",
    "shadcn": "^4.11.0",
    "wrangler": "^4.105.0"
  },
  "scripts": {
    "build": "npm run build --prefix car-wash-frontend",
    "deploy": "npm run build && wrangler deploy",
    "preview": "wrangler dev"
  }
}
