{
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0",
    "wrangler": "^4.105.0"
  },
  "scripts": {
    "build": "npm ci --prefix car-wash-frontend && npm run build --prefix car-wash-frontend",
    "deploy": "npm run build && wrangler deploy",
    "preview": "wrangler dev",
    "test:load": "k6 run tests/load/k6/booking-flow.js",
    "test:load:heavy": "PROFILE=load VUS=30 DURATION=2m k6 run tests/load/k6/booking-flow.js"
  }
}
